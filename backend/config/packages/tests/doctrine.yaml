doctrine:
    dbal:
        # "TEST_DB_URL" is an environment variable defined in phpunit.xml.dist or .env.test
        url: '%env(resolve:TEST_DB_URL)%'
        options:
          1002: "SET FOREIGN_KEY_CHECKS=1"