{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-slate-600 py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full bg-white shadow-md rounded-lg p-8">
            <div>
                <h2 class="text-center text-3xl font-extrabold text-gray-900">
                    Email envoyé
                </h2>
                <p class="mt-4 text-center text-gray-600">
                    Si un compte existe avec cette adresse email, vous recevrez un email contenant un lien pour réinitialiser votre mot de passe.
                    Ce lien expirera dans 1 heure.
                </p>
                <div class="mt-6 text-center">
                    <p class="text-gray-600">
                        Si vous ne recevez pas d'email, veuillez vérifier votre dossier spam ou 
                        <a href="{{ path('app_forgot_password_request') }}" class="text-sky-600 hover:text-sky-500">
                            essayer à nouveau
                        </a>
                    </p>
                    <div class="mt-4">
                        <a href="{{ path('app_login') }}" class="text-sky-600 hover:text-sky-500">
                            Retour à la connexion
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
