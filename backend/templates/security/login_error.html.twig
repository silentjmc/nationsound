{% extends 'base.html.twig' %}

{% block title %}Erreur de connexion{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center bg-slate-600">
    <div class="max-w-md w-full bg-white p-8 rounded-lg shadow-md">
        {% if error_type == 'not_verified' %}
            <div class="mb-4 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-yellow-100 mb-4">
                    <i class="fas fa-exclamation-triangle text-yellow-500 text-2xl"></i>
                </div>
                <h2 class="text-2xl font-bold text-gray-800 mb-2">Compte non vérifié</h2>
                <p class="text-gray-600">Votre compte n'a pas encore été vérifié par un administrateur. Veuillez patienter ou contacter l'administrateur.</p>
            </div>
        {% else %}
            <div class="mb-4 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-red-100 mb-4">
                    <i class="fas fa-times text-red-500 text-2xl"></i>
                </div>
                <h2 class="text-2xl font-bold text-gray-800 mb-2">Identifiants invalides</h2>
                <p class="text-gray-600">L'email ou le mot de passe que vous avez saisi est incorrect.</p>
            </div>
        {% endif %}

        <div class="flex items-center justify-between mt-6">
            <a href="{{ path('app_login') }}" class="px-4 py-2 bg-sky-600 text-white text-sm font-medium rounded-md hover:bg-sky-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-sky-500">
                Retour à la connexion
            </a>
            <a href="{{ path('app_forgot_password_request') }}" class="text-sm text-sky-600 hover:text-sky-500">
                Mot de passe oublié ?
            </a>
        </div>
    </div>
</div>
{% endblock %}
