{% extends 'base.html.twig' %}

{% block title %}Token invalide - Nation Sound{% endblock %}

{% block body %}
<div class="min-h-screen bg-slate-600 flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8 bg-white p-10 rounded-xl shadow-2xl">
        <div class="text-center">
            <img class="mx-auto h-24 w-auto rounded-lg border border-black/10" 
                 src="{{ asset('images/logo_ns_rect_txtw.png') }}" 
                 alt="Nation Sound">
            
            <div class="mt-6">
                <svg class="mx-auto h-12 w-12 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                </svg>
                
                <h2 class="mt-4 text-3xl font-bold text-gray-900">
                    Token invalide
                </h2>
                
                <p class="mt-2 text-sm text-gray-600">
                    Le lien de validation que vous avez utilisé n'est plus valide ou a déjà été utilisé.
                </p>
            </div>
        </div>

        <div class="mt-8">
            <a href="{{ path('app_login') }}" 
               class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-600 transition-colors duration-200">
                Retourner à la page de connexion
            </a>
        </div>
    </div>
</div>
{% endblock %}
