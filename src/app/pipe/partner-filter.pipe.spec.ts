/*
import { PartnerFilterPipe } from './partner-filter.pipe';

describe('PartnerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PartnerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
*/
